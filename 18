Block block2("Block 2 Data", blockchain.getLatestBlock().getHash());
    blockchain.addBlock(block2);

    Block block3("Block 3 Data", blockchain.getLatestBlock().getHash());
    blockchain.addBlock(block3);
