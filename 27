 // Add more blocks
    Block newBlock1 = mineBlock(blockchain.back(), "Block 1 Data");
    blockchain.push_back(newBlock1);

    Block newBlock2 = mineBlock(blockchain.back(), "Block 2 Data");
    blockchain.push_back(newBlock2);
